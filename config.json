{
    "listen": "${HOST}",
    "tmp_path": "/tmp",
    "web_root": "${ROOT}",
    "webkit_bin": "/usr/bin/phantomjs",
	"cache_ttl": ${TLL},
    "worker": ${WORKER},
    "timeout": ${TIMEOUT},
    "webkit_args": [ "/app/render/pdf.js" ]
}